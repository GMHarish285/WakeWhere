auto stop tracking on reaching location
make track button work
change app logo
search feature in map
clean repo
add light/dark theme toggle
use curr location feature in map
persistent notification on newer android

publish
create eas workflow

npx expo prebuild --platform android --clean
npx eas build -p android --profile preview