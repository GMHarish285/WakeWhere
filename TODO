change "config" to "destination", "stop" or "location"
auto stop tracking on reaching location
dismissable alarm instead of sound notification
when app is closed when a tracking is active, bg task is running but tracking status ui says not tracking
